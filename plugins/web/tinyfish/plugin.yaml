name: web-tinyfish
version: 1.0.0
description: "TinyFish (Free) web search + content extraction. Requires TINYFISH_API_KEY — free key at https://agent.tinyfish.ai/api-keys."
author: NousResearch
kind: backend
provides_web_providers:
  - tinyfish
